name: blast
channels:
  - bioconda
  - conda-forge
dependencies:
  - python>=3.8
  - orthofinder
  - blast
  - cd-hit
  #- interproscan this requires huge space in the disk
  - pip:
    - tmhmm.py
    - deepsig-biocomp