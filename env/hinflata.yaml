name: hinflata
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.8
  - biopython
  - blast
  - cd-hit
  - cdhit-reader
  - orthofinder
  - trnascan-se
  - samtools
  - bedtools
  - bioconda::barrnap
  #- interproscan this requires huge space in the disk
  #- pip:
  #  - tmhmm.py
  #  - deepsig-biocomp